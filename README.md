# Spacefast Docs

The documentation site for [Spacefast](https://spacefast.com/docs). Guides live in `content/`; everything under `generated/` is a verified snapshot — don't edit it by hand.

## Development

Requires Bun 1.3.11 and Node.js 24+.

```bash
bun install --frozen-lockfile
bun run dev
```

Before opening a PR:

```bash
bun run check
```

## Contributing

Treat this repository and its full history as public. Contributor rules and the public-safety gate are documented in [AGENTS.md](AGENTS.md).
