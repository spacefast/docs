---
title: Image acceleration
description: Resize, crop, filter, and serve public images through Site Accelerator.
---

Serve public images through Site Accelerator for content delivery network
(CDN) caching and on-the-fly transforms. Append query parameters to a URL to
resize, crop, filter, and compress an image. You do not need a build step.

## URL format

Use `i0.wp.com` followed by the original public image host and path. Images
must be public over HTTP or HTTPS.

```text
https://i0.wp.com/{original-image-host}/{path-to-image}?{params}
```

For example:

```text
https://i0.wp.com/example.com/images/hero.jpg?w=1200&quality=80
```

## Transform gallery

Resize to a target width with `w`:

```text
https://i0.wp.com/s.ma.tt/files/2012/06/DSC01205.jpg?w=300
```

![Theater sign resized with w=300](https://i0.wp.com/s.ma.tt/files/2012/06/DSC01205.jpg?w=300)

Crop by percentage offsets and dimensions with `crop`:

```text
https://i0.wp.com/s.ma.tt/files/2012/06/4-MCM_0830-1600x1064.jpg?crop=12,25,60,60
```

![Crop using percentage offsets and dimensions](https://i0.wp.com/s.ma.tt/files/2012/06/4-MCM_0830-1600x1064.jpg?crop=12,25,60,60)

Resize and crop to exact dimensions with `resize`:

```text
https://i0.wp.com/s.ma.tt/files/2012/06/9-DSC01406-1600x1066.jpg?resize=400,220
```

![Landscape resized and cropped to 400 by 220](https://i0.wp.com/s.ma.tt/files/2012/06/9-DSC01406-1600x1066.jpg?resize=400,220)

Stay inside a bounding box without changing aspect ratio with `fit`:

```text
https://i0.wp.com/s.ma.tt/files/2010/10/MCM_4049.jpg?fit=300,300
```

![Landscape fit within 300 by 300](https://i0.wp.com/s.ma.tt/files/2010/10/MCM_4049.jpg?fit=300,300)

Apply a filter with `filter`:

```text
https://i0.wp.com/s.ma.tt/files/2010/12/MCM_5875-1600x1064.jpg?w=200&filter=grayscale
```

![Grayscale filter](https://i0.wp.com/s.ma.tt/files/2010/12/MCM_5875-1600x1064.jpg?w=200&filter=grayscale)

Add black letterboxing while scaling with `lb`:

```text
https://images.spacefast.com/examples/letterbox.jpg?lb=310,250
```

![Letterbox scaled to 310 by 250](https://images.spacefast.com/examples/letterbox.jpg?lb=310,250)

## Query parameters

| Parameter     | Description                                                                                                                                      | Example                                                |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| `w`           | Set the output width in pixels.                                                                                                                  | `?w=300`                                               |
| `h`           | Set the output height in pixels.                                                                                                                 | `?h=200`                                               |
| `crop`        | Crop by percentage offsets and dimensions, or by pixel values. Values are `x,y,w,h`.                                                             | `?crop=12,25,60,60` or `?crop=160px,160px,788px,788px` |
| `resize`      | Resize and crop to exact `width,height` dimensions while preserving as much of the image as possible.                                            | `?resize=400,220`                                      |
| `fit`         | Fit within a `width,height` bounding box while preserving aspect ratio.                                                                          | `?fit=300,300`                                         |
| `lb`          | Add black letterboxing while scaling to `width,height`.                                                                                          | `?lb=310,250`                                          |
| `ulb`         | Remove black letterboxing.                                                                                                                       | `?ulb=true`                                            |
| `filter`      | Apply a filter. Supported values are `negate`, `grayscale`, `sepia`, `edgedetect`, `emboss`, `blurgaussian`, `blurselective`, and `meanremoval`. | `?filter=grayscale`                                    |
| `brightness`  | Adjust brightness from `-255` through `255`.                                                                                                     | `?brightness=40`                                       |
| `contrast`    | Adjust contrast from `-100` through `100`.                                                                                                       | `?contrast=50`                                         |
| `colorize`    | Add a color tint with comma-separated RGB values.                                                                                                | `?colorize=100,0,0`                                    |
| `smooth`      | Smooth the image. Lower values increase smoothing; `0` is maximum smoothing.                                                                     | `?smooth=2`                                            |
| `zoom`        | Size images for high pixel ratio devices. Valid values are `1`, `1.5`, and `2` through `10`. You cannot use `zoom` with `crop`.                  | `?zoom=2`                                              |
| `quality`     | Control output quality from `10` through `100`. If you omit `quality`, Site Accelerator tries to preserve the original quality.                  | `?quality=80`                                          |
| `allow_lossy` | Allow lossy output such as JPEG or WebP when the source is lossless and the browser supports it.                                                 | `?allow_lossy=1`                                       |
| `strip`       | Control metadata. `all` strips extraneous metadata and is the default; `none` preserves Exif, IPTC, and XMP metadata.                            | `?strip=none`                                          |

## Supported formats

Site Accelerator serves common raster image formats such as `.gif`, `.png`,
`.jpg`, and `.webp`. When the request and the browser support it, Site
Accelerator converts to more efficient lossy output (WebP).

## URL helper package

When a framework integration needs more than width and quality, use
`@spacefast/image`. It always emits `i0.wp.com` URLs instead of
distributing requests across hosts, so repeated requests for the same
transformed image share one cache key.

Install it:

```package-install
bun add @spacefast/image
```

```ts
import { siteAcceleratorUrl } from "@spacefast/image";

const src = siteAcceleratorUrl("https://example.com/uploads/hero.jpg", {
  resize: [1200, 630],
  quality: 80,
});
```

## Limitations

- Images must be public. Private upload protection cannot use this path,
  because Site Accelerator must fetch the original image.
- Site Accelerator does not upscale beyond the original image dimensions.
- Site Accelerator keys cached images by image URL. When an image changes,
  change the filename or the path so the edge cache fetches the new asset.
  Do not rely on query-string cache busters on the original image URL.
- Site Accelerator is for images and static assets. It does not serve
  video, audio, or other media formats as image transforms.
- On large images, Site Accelerator sometimes skips heavy filters such as
  `brightness`, `contrast`, `colorize`, and `smooth`.

## See also

- [Next.js integration](/guides/frameworks/nextjs)
