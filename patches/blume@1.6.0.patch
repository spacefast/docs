diff --git a/dist/cli/index.js b/dist/cli/index.js
index 908d4c4e89bbbc6496b688c1e6e3fd6bf5e1b235..79a5cde00da6e42e1f4cf81004556c12ac929916 100755
--- a/dist/cli/index.js
+++ b/dist/cli/index.js
@@ -13056,7 +13056,7 @@ var CASE_BOUNDARY = /(?<before>[\p{Ll}\p{N}])(?<capital>\p{Lu})/gu;
 var ACRONYM_BOUNDARY = /(?<acronym>\p{Lu})(?<word>\p{Lu}\p{Ll})/gu;
 var splitIdentifier = (identifier) => identifier.replace(CASE_BOUNDARY, "$<before>-$<capital>").replace(ACRONYM_BOUNDARY, "$<acronym>-$<word>");
 var operationKey = (method, path, operationId) => {
-  const fromId = operationId ? slugify(splitIdentifier(operationId)) : "";
+  const fromId = operationId ? slugify(operationId) : "";
   return fromId || slugify(`${method}-${path}`);
 };
 var isOperation = (value) => typeof value === "object" && value !== null;
diff --git a/src/openapi/model.ts b/src/openapi/model.ts
index c80375a55a02bd7715f07c9cd689c3f716dc3263..0f6c2ebed56381017615bd65613ee575b05774ec 100644
--- a/src/openapi/model.ts
+++ b/src/openapi/model.ts
@@ -67,7 +67,8 @@ export const operationKey = (
   path: string,
   operationId?: string
 ): string => {
-  const fromId = operationId ? slugify(splitIdentifier(operationId)) : "";
+  // Published OpenAPI reference URLs are stable across dependency upgrades.
+  const fromId = operationId ? slugify(operationId) : "";
   return fromId || slugify(`${method}-${path}`);
 };
 
