diff --git a/dist/cli/index.js b/dist/cli/index.js
index eb5fcc4ac5b1ceb255580c252d059c531881994c..7adf5c1dca7b7f69eec238cab714f15770565bc1 100755
--- a/dist/cli/index.js
+++ b/dist/cli/index.js
@@ -3090,7 +3090,12 @@ var routeSlug = (route) => slugify(trimChar(route, "/")) || "reference";
 var sourcesOf = (block) => {
   const sources = [...block.sources];
   if (block.spec) {
-    sources.unshift({ spec: block.spec });
+    sources.unshift({
+      includeInLlms: true,
+      includeInSearch: true,
+      noindex: false,
+      spec: block.spec
+    });
   }
   return sources;
 };
@@ -3114,8 +3119,11 @@ var referencesFor = (kind, block, defaultLabel, renderer, display, basePath) =>
     return {
       basePath,
       display,
+      includeInLlms: source.includeInLlms,
+      includeInSearch: source.includeInSearch,
       kind,
       label,
+      noindex: source.noindex,
       renderer,
       route,
       scalar: block.scalar,
@@ -3444,7 +3452,7 @@ var runtimeDependencies = (options) => {
     deps.push("@scalar/astro");
   }
   deps.push(...searchProviderMeta(config.search.provider).runtimeDeps);
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     const askDep = askBackendRuntimeDep(config.ai.ask);
     if (askDep) {
       deps.push(askDep);
@@ -3811,7 +3819,11 @@ import data from "blume:data";
 const { strings } = Astro.props;
 ---
 
-<AskAI strings={strings ?? data.ui.ask} suggestions={data.config.ask?.suggestions ?? []} />
+<AskAI
+  endpoint={data.config.ask?.endpoint ?? undefined}
+  strings={strings ?? data.ui.ask}
+  suggestions={data.config.ask?.suggestions ?? []}
+/>
 ` : `---
 // Generated by Blume. Do not edit.
 // Ask AI is off (\`ai.ask.enabled\`), so the header's Ask trigger renders nothing.
@@ -7514,6 +7526,9 @@ var searchMetaSchema = z2.strictObject({
   exclude: z2.boolean().default(false),
   tags: z2.array(z2.string()).optional()
 });
+var aiMetaSchema = z2.strictObject({
+  exclude: z2.boolean().default(false)
+});
 var changelogMetaSchema = z2.strictObject({
   category: z2.string().optional(),
   date: dateSchema.optional(),
@@ -7529,6 +7544,7 @@ var authorSchema = z2.union([
   }).catchall(z2.unknown())
 ]);
 var pageMetaBaseSchema = z2.strictObject({
+  ai: aiMetaSchema.default({}),
   authors: z2.union([authorSchema, z2.array(authorSchema)]).optional(),
   changelog: changelogMetaSchema.optional(),
   date: dateSchema.optional(),
@@ -7811,6 +7827,7 @@ var aiConfigSchema = z2.strictObject({
     apiKeyEnv: z2.string().optional(),
     baseUrl: z2.string().url().optional(),
     enabled: z2.boolean().default(false),
+    endpoint: z2.string().min(1).optional(),
     model: z2.string().default("openai/gpt-5.5"),
     provider: z2.enum(askAiProviders).default("gateway"),
     suggestions: z2.array(z2.strictObject({
@@ -7818,7 +7835,7 @@ var aiConfigSchema = z2.strictObject({
       label: z2.string().min(1)
     })).default([])
   }).superRefine((value, ctx) => {
-    if (value.provider === "openai-compatible" && !value.baseUrl) {
+    if (value.provider === "openai-compatible" && !(value.baseUrl || value.endpoint)) {
       ctx.addIssue({
         code: z2.ZodIssueCode.custom,
         message: 'ai.ask.baseUrl is required when provider is "openai-compatible".',
@@ -8046,7 +8063,10 @@ var reactConfigSchema = z2.strictObject({
   compiler: z2.boolean().default(true)
 });
 var openapiSourceSchema = z2.strictObject({
+  includeInLlms: z2.boolean().default(true),
+  includeInSearch: z2.boolean().default(true),
   label: z2.string().optional(),
+  noindex: z2.boolean().default(false),
   route: z2.string().optional(),
   spec: z2.string()
 });
@@ -9918,23 +9938,30 @@ var operationDescription = (spec, operation) => {
 var withDescription = (description, component) => description.trim() ? `${mdxSafe(description.trim())}
 
 ${component}` : component;
-var operationMdx = (spec, operation) => {
+var operationMdx = (spec, operation, reference) => {
   const method = operation.method.toUpperCase();
   const title = operation.summary || `${method} ${operation.path}`;
   const description = operation.description.trim() === operation.summary.trim() ? "" : operation.description;
   return {
     body: withDescription(description, `<Operation source="${spec.slug}" id="${operation.key}" />`),
     data: {
+      ...reference?.includeInLlms === false ? { ai: { exclude: true } } : {},
       ...operation.deprecated ? { deprecated: true } : {},
-      search: { tags: [operation.tag, method] },
-      seo: { description: operationDescription(spec, operation) },
+      search: {
+        ...reference?.includeInSearch === false ? { exclude: true } : {},
+        tags: [operation.tag, method]
+      },
+      seo: {
+        description: operationDescription(spec, operation),
+        ...reference?.noindex ? { noindex: true } : {}
+      },
       sidebar: { badge: method, label: operation.summary || operation.path },
       title,
       type: "openapi-operation"
     }
   };
 };
-var overviewMdx = (spec) => {
+var overviewMdx = (spec, reference) => {
   const operations = Object.values(spec.operations);
   const sections = [];
   const known = new Set;
@@ -9976,8 +10003,11 @@ var overviewMdx = (spec) => {
 
 `),
     data: {
+      ...reference?.includeInLlms === false ? { ai: { exclude: true } } : {},
+      ...reference?.includeInSearch === false ? { search: { exclude: true } } : {},
       seo: {
-        description: clip(plainProse(spec.description), META_DESCRIPTION_MAX) || `${apiName(spec)} API reference.`
+        description: clip(plainProse(spec.description), META_DESCRIPTION_MAX) || `${apiName(spec)} API reference.`,
+        ...reference?.noindex ? { noindex: true } : {}
       },
       sidebar: { label: "Overview" },
       title: apiName(spec)
@@ -9999,10 +10029,10 @@ var toEntry = (rendered, ref) => {
     ref
   };
 };
-var specEntries = (spec, operations) => {
-  const entries = operations.map((operation) => toEntry(operationMdx(spec, operation), `${routeToRef(operation.route)}.mdx`));
+var specEntries = (spec, operations, reference) => {
+  const entries = operations.map((operation) => toEntry(operationMdx(spec, operation, reference), `${routeToRef(operation.route)}.mdx`));
   const base = routeToRef(spec.route);
-  entries.push(toEntry(overviewMdx(spec), base ? `${base}/index.mdx` : "index.mdx"));
+  entries.push(toEntry(overviewMdx(spec, reference), base ? `${base}/index.mdx` : "index.mdx"));
   return entries;
 };
 var openApiSource = (references, ctx) => {
@@ -10056,7 +10086,7 @@ var openApiSource = (references, ctx) => {
             }
           ] : []
         ],
-        entries: specEntries(spec, operations),
+        entries: specEntries(spec, operations, reference),
         slug: reference.slug,
         spec
       };
@@ -11607,7 +11637,7 @@ var buildAgentReadability = (project) => {
     };
   }
   if (config.ai.ask?.enabled) {
-    artifacts.askApi = abs("/api/ask");
+    artifacts.askApi = config.ai.ask.endpoint ?? abs("/api/ask");
   }
   if (site && config.seo.sitemap) {
     artifacts.sitemap = abs("/sitemap.xml");
@@ -11950,7 +11980,7 @@ var pageUrl = (route, site, base = "") => {
   const path = withBasePath(base, route);
   return encodeURI(site ? `${site.replace(/\/$/u, "")}${path}` : path);
 };
-var eligiblePages = (project) => project.graph.pages.filter((page) => !(page.meta.draft || page.meta.sidebar.hidden || page.meta.seo.noindex) && (project.config.ai.llmsTxt.openapi || page.source.name !== "openapi"));
+var eligiblePages = (project) => project.graph.pages.filter((page) => !(page.meta.ai.exclude || page.meta.draft || page.meta.sidebar.hidden || page.meta.seo.noindex) && (project.config.ai.llmsTxt.openapi || page.source.name !== "openapi"));
 var indexedNavigations = (project) => {
   const { i18n } = project.config;
   if (i18n) {
@@ -12099,7 +12129,7 @@ var ensureGitignore = async (root, entries) => {
 // src/core/server-features.ts
 var serverFeatures = (config) => {
   const features = [];
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     features.push("Ask AI");
   }
   if (config.ai.mcp.enabled) {
@@ -14983,7 +15013,10 @@ var buildRuntimeData = (project) => {
     config: {
       analytics: config.analytics ?? null,
       appleIcon: resolveAppleIcon(project),
-      ask: config.ai.ask?.enabled ? { suggestions: config.ai.ask.suggestions } : null,
+      ask: config.ai.ask?.enabled ? {
+        endpoint: config.ai.ask.endpoint ?? null,
+        suggestions: config.ai.ask.suggestions
+      } : null,
       banner: resolveBanner(config),
       basePath: config.basePath,
       codeThemes: config.markdown.codeBlocks.theme,
@@ -15115,7 +15148,7 @@ var writeMcpFiles = async (project, plan, write) => {
 };
 var writeAskFiles = async (project, srcDir, write) => {
   const { ask } = project.config.ai;
-  if (!ask?.enabled) {
+  if (!(ask?.enabled && !ask.endpoint)) {
     return;
   }
   const grounded = ask.provider !== "inkeep";
@@ -15439,7 +15472,7 @@ var checkRequiredSecrets = (config) => {
       suggestion: `Set ${env} in .env.local for local dev, or in your host's environment for production.`
     });
   };
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     const backend = resolveAskBackend(config.ai.ask);
     if (backend.kind === "gateway") {
       requireSecret("Ask AI (AI Gateway)", "AI_GATEWAY_API_KEY", "on Vercel the gateway can also authenticate via OIDC");
@@ -16196,7 +16229,7 @@ var ejectOpenApiData = (project) => {
 };
 var askFiles = async (project, srcDir, genDir) => {
   const { ask } = project.config.ai;
-  if (!ask?.enabled) {
+  if (!(ask?.enabled && !ask.endpoint)) {
     return [];
   }
   const grounded = ask.provider !== "inkeep";
@@ -17474,5 +17507,5 @@ process.on("unhandledRejection", (error) => {
 });
 runMain(main);
 
-//# debugId=5D342B2E16EE6E0464756E2164756E21
+//# debugId=8DE462207183543D64756E2164756E21
 //# sourceMappingURL=index.js.map
diff --git a/dist/types/core/config-input.d.ts b/dist/types/core/config-input.d.ts
index 9b0180900c4555ed10efb386b9f67a50b68cbec5..7efb409435a150833ee80d491f975bfcae9cb79d 100644
--- a/dist/types/core/config-input.d.ts
+++ b/dist/types/core/config-input.d.ts
@@ -444,6 +444,12 @@ export interface AskConfig {
     baseUrl?: string;
     /** Turn Ask AI on. Defaults to `false`. */
     enabled?: boolean;
+    /**
+     * Existing Ask AI endpoint to call instead of generating one. This keeps a
+     * Blume site static while an API backend owns retrieval, model access, rate
+     * limiting, and streaming. Accepts an absolute URL or root-relative path.
+     */
+    endpoint?: string;
     /** Model id to use. Defaults to `openai/gpt-5.5`. */
     model?: string;
     /** Which backend routes the request. Defaults to `gateway`. */
diff --git a/dist/types/core/data.d.ts b/dist/types/core/data.d.ts
index 0bb97b3640021a5b205b6fca00dba7c130661fe2..b2b524949a5908c55edb6aa519f0142540a22a7a 100644
--- a/dist/types/core/data.d.ts
+++ b/dist/types/core/data.d.ts
@@ -95,6 +95,7 @@ export interface BlumeDataConfig {
     appleIcon: BlumeFavicon | null;
     /** Ask AI empty-state suggestions, or `null` when Ask AI is off. */
     ask: {
+        endpoint: string | null;
         suggestions: NonNullable<ResolvedConfig["ai"]["ask"]>["suggestions"];
     } | null;
     banner: BlumeBanner | null;
diff --git a/dist/types/core/schema.d.ts b/dist/types/core/schema.d.ts
index a6eac8646a05a65e958a7f8345ab93a9da7dd9d7..e90a10d2522883d9a9ad9c3cf3d378494307517d 100644
--- a/dist/types/core/schema.d.ts
+++ b/dist/types/core/schema.d.ts
@@ -7,6 +7,14 @@ declare const hydrationMode: z.ZodEnum<["load", "idle", "visible", "media", "onl
 export type HydrationMode = z.infer<typeof hydrationMode>;
 /** Frontmatter accepted on any content page. */
 declare const pageMetaBaseSchema: z.ZodObject<{
+    ai: z.ZodDefault<z.ZodObject<{
+        /** Exclude this page from llms.txt and llms-full.txt. */
+        exclude: z.ZodDefault<z.ZodBoolean>;
+    }, "strict", z.ZodTypeAny, {
+        exclude: boolean;
+    }, {
+        exclude?: boolean | undefined;
+    }>>;
     /** Post author(s) for blog/changelog content; preserved, not yet rendered. */
     authors: z.ZodOptional<z.ZodUnion<[z.ZodUnion<[z.ZodString, z.ZodObject<{
         avatar: z.ZodOptional<z.ZodString>;
@@ -99,6 +107,7 @@ declare const pageMetaBaseSchema: z.ZodObject<{
         } | undefined;
         canonical?: string | undefined;
     }, {
+        noindex?: boolean | undefined;
         description?: string | undefined;
         title?: string | undefined;
         image?: string | undefined;
@@ -106,7 +115,6 @@ declare const pageMetaBaseSchema: z.ZodObject<{
             creator?: string | undefined;
         } | undefined;
         canonical?: string | undefined;
-        noindex?: boolean | undefined;
     }>>;
     sidebar: z.ZodDefault<z.ZodObject<{
         badge: z.ZodOptional<z.ZodString>;
@@ -131,6 +139,10 @@ declare const pageMetaBaseSchema: z.ZodObject<{
     title: z.ZodOptional<z.ZodString>;
     type: z.ZodOptional<z.ZodString>;
 }, "strict", z.ZodTypeAny, {
+    ai: {
+        exclude: boolean;
+    };
+    noindex: boolean;
     sidebar: {
         hidden: boolean;
         icon?: string | undefined;
@@ -155,7 +167,6 @@ declare const pageMetaBaseSchema: z.ZodObject<{
     };
     deprecated: boolean;
     hidden: boolean;
-    noindex: boolean;
     draft: boolean;
     type?: string | undefined;
     date?: string | undefined;
@@ -184,6 +195,10 @@ declare const pageMetaBaseSchema: z.ZodObject<{
     type?: string | undefined;
     date?: string | Date | undefined;
     icon?: string | undefined;
+    ai?: {
+        exclude?: boolean | undefined;
+    } | undefined;
+    noindex?: boolean | undefined;
     description?: string | undefined;
     lastModified?: string | Date | undefined;
     slug?: string | undefined;
@@ -206,6 +221,7 @@ declare const pageMetaBaseSchema: z.ZodObject<{
         category?: string | undefined;
     } | undefined;
     seo?: {
+        noindex?: boolean | undefined;
         description?: string | undefined;
         title?: string | undefined;
         image?: string | undefined;
@@ -213,11 +229,9 @@ declare const pageMetaBaseSchema: z.ZodObject<{
             creator?: string | undefined;
         } | undefined;
         canonical?: string | undefined;
-        noindex?: boolean | undefined;
     } | undefined;
     deprecated?: boolean | undefined;
     hidden?: boolean | undefined;
-    noindex?: boolean | undefined;
     authors?: string | z.objectInputType<{
         avatar: z.ZodOptional<z.ZodString>;
         image: z.ZodOptional<z.ZodString>;
@@ -232,6 +246,14 @@ declare const pageMetaBaseSchema: z.ZodObject<{
     draft?: boolean | undefined;
 }>;
 export declare const pageMetaSchema: z.ZodObject<{
+    ai: z.ZodDefault<z.ZodObject<{
+        /** Exclude this page from llms.txt and llms-full.txt. */
+        exclude: z.ZodDefault<z.ZodBoolean>;
+    }, "strict", z.ZodTypeAny, {
+        exclude: boolean;
+    }, {
+        exclude?: boolean | undefined;
+    }>>;
     /** Post author(s) for blog/changelog content; preserved, not yet rendered. */
     authors: z.ZodOptional<z.ZodUnion<[z.ZodUnion<[z.ZodString, z.ZodObject<{
         avatar: z.ZodOptional<z.ZodString>;
@@ -324,6 +346,7 @@ export declare const pageMetaSchema: z.ZodObject<{
         } | undefined;
         canonical?: string | undefined;
     }, {
+        noindex?: boolean | undefined;
         description?: string | undefined;
         title?: string | undefined;
         image?: string | undefined;
@@ -331,7 +354,6 @@ export declare const pageMetaSchema: z.ZodObject<{
             creator?: string | undefined;
         } | undefined;
         canonical?: string | undefined;
-        noindex?: boolean | undefined;
     }>>;
     sidebar: z.ZodDefault<z.ZodObject<{
         badge: z.ZodOptional<z.ZodString>;
@@ -356,6 +378,10 @@ export declare const pageMetaSchema: z.ZodObject<{
     title: z.ZodOptional<z.ZodString>;
     type: z.ZodOptional<z.ZodString>;
 }, "strict", z.ZodTypeAny, {
+    ai: {
+        exclude: boolean;
+    };
+    noindex: boolean;
     sidebar: {
         hidden: boolean;
         icon?: string | undefined;
@@ -380,7 +406,6 @@ export declare const pageMetaSchema: z.ZodObject<{
     };
     deprecated: boolean;
     hidden: boolean;
-    noindex: boolean;
     draft: boolean;
     type?: string | undefined;
     date?: string | undefined;
@@ -409,6 +434,10 @@ export declare const pageMetaSchema: z.ZodObject<{
     type?: string | undefined;
     date?: string | Date | undefined;
     icon?: string | undefined;
+    ai?: {
+        exclude?: boolean | undefined;
+    } | undefined;
+    noindex?: boolean | undefined;
     description?: string | undefined;
     lastModified?: string | Date | undefined;
     slug?: string | undefined;
@@ -431,6 +460,7 @@ export declare const pageMetaSchema: z.ZodObject<{
         category?: string | undefined;
     } | undefined;
     seo?: {
+        noindex?: boolean | undefined;
         description?: string | undefined;
         title?: string | undefined;
         image?: string | undefined;
@@ -438,11 +468,9 @@ export declare const pageMetaSchema: z.ZodObject<{
             creator?: string | undefined;
         } | undefined;
         canonical?: string | undefined;
-        noindex?: boolean | undefined;
     } | undefined;
     deprecated?: boolean | undefined;
     hidden?: boolean | undefined;
-    noindex?: boolean | undefined;
     authors?: string | z.objectInputType<{
         avatar: z.ZodOptional<z.ZodString>;
         image: z.ZodOptional<z.ZodString>;
@@ -746,6 +774,7 @@ declare const aiConfigSchema: z.ZodObject<{
         apiKeyEnv: z.ZodOptional<z.ZodString>;
         baseUrl: z.ZodOptional<z.ZodString>;
         enabled: z.ZodDefault<z.ZodBoolean>;
+        endpoint: z.ZodOptional<z.ZodString>;
         model: z.ZodDefault<z.ZodString>;
         provider: z.ZodDefault<z.ZodEnum<["gateway", "openrouter", "llmgateway", "inkeep", "openai-compatible"]>>;
         suggestions: z.ZodDefault<z.ZodArray<z.ZodObject<{
@@ -768,10 +797,12 @@ declare const aiConfigSchema: z.ZodObject<{
         }[];
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
+        endpoint?: string | undefined;
     }, {
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
         enabled?: boolean | undefined;
+        endpoint?: string | undefined;
         model?: string | undefined;
         provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
         suggestions?: {
@@ -788,10 +819,12 @@ declare const aiConfigSchema: z.ZodObject<{
         }[];
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
+        endpoint?: string | undefined;
     }, {
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
         enabled?: boolean | undefined;
+        endpoint?: string | undefined;
         model?: string | undefined;
         provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
         suggestions?: {
@@ -866,12 +899,14 @@ declare const aiConfigSchema: z.ZodObject<{
         }[];
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
+        endpoint?: string | undefined;
     } | undefined;
 }, {
     ask?: {
         apiKeyEnv?: string | undefined;
         baseUrl?: string | undefined;
         enabled?: boolean | undefined;
+        endpoint?: string | undefined;
         model?: string | undefined;
         provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
         suggestions?: {
@@ -1083,13 +1118,22 @@ declare const dateFormatConfigSchema: z.ZodEffects<z.ZodObject<{
  * `http(s)` URL (OpenAPI for the Blume renderer; OpenAPI or AsyncAPI for Scalar).
  */
 declare const openapiSourceSchema: z.ZodObject<{
+    /** Include generated pages from this spec in llms.txt/llms-full.txt. */
+    includeInLlms: z.ZodDefault<z.ZodBoolean>;
+    /** Include generated pages from this spec in site search. */
+    includeInSearch: z.ZodDefault<z.ZodBoolean>;
     /** Nav/section label for this source. */
     label: z.ZodOptional<z.ZodString>;
+    /** Emit noindex metadata and omit generated pages from the sitemap. */
+    noindex: z.ZodDefault<z.ZodBoolean>;
     /** Per-source route; defaults to the block's `route` (or a derived path). */
     route: z.ZodOptional<z.ZodString>;
     /** Local path or `http(s)` URL to the spec. */
     spec: z.ZodString;
 }, "strict", z.ZodTypeAny, {
+    includeInLlms: boolean;
+    includeInSearch: boolean;
+    noindex: boolean;
     spec: string;
     label?: string | undefined;
     route?: string | undefined;
@@ -1097,14 +1141,18 @@ declare const openapiSourceSchema: z.ZodObject<{
     spec: string;
     label?: string | undefined;
     route?: string | undefined;
+    includeInLlms?: boolean | undefined;
+    includeInSearch?: boolean | undefined;
+    noindex?: boolean | undefined;
 }>;
-export type OpenApiSource = z.infer<typeof openapiSourceSchema>;
+export type OpenApiSource = z.input<typeof openapiSourceSchema>;
 export declare const blumeConfigSchema: z.ZodObject<{
     ai: z.ZodDefault<z.ZodObject<{
         ask: z.ZodOptional<z.ZodEffects<z.ZodObject<{
             apiKeyEnv: z.ZodOptional<z.ZodString>;
             baseUrl: z.ZodOptional<z.ZodString>;
             enabled: z.ZodDefault<z.ZodBoolean>;
+            endpoint: z.ZodOptional<z.ZodString>;
             model: z.ZodDefault<z.ZodString>;
             provider: z.ZodDefault<z.ZodEnum<["gateway", "openrouter", "llmgateway", "inkeep", "openai-compatible"]>>;
             suggestions: z.ZodDefault<z.ZodArray<z.ZodObject<{
@@ -1127,10 +1175,12 @@ export declare const blumeConfigSchema: z.ZodObject<{
             }[];
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
+            endpoint?: string | undefined;
         }, {
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
             enabled?: boolean | undefined;
+            endpoint?: string | undefined;
             model?: string | undefined;
             provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
             suggestions?: {
@@ -1147,10 +1197,12 @@ export declare const blumeConfigSchema: z.ZodObject<{
             }[];
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
+            endpoint?: string | undefined;
         }, {
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
             enabled?: boolean | undefined;
+            endpoint?: string | undefined;
             model?: string | undefined;
             provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
             suggestions?: {
@@ -1225,12 +1277,14 @@ export declare const blumeConfigSchema: z.ZodObject<{
             }[];
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
+            endpoint?: string | undefined;
         } | undefined;
     }, {
         ask?: {
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
             enabled?: boolean | undefined;
+            endpoint?: string | undefined;
             model?: string | undefined;
             provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
             suggestions?: {
@@ -1319,13 +1373,22 @@ export declare const blumeConfigSchema: z.ZodObject<{
         /** Extra Scalar config forwarded to `<ScalarComponent>`. */
         scalar: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnknown>>;
         sources: z.ZodDefault<z.ZodArray<z.ZodObject<{
+            /** Include generated pages from this spec in llms.txt/llms-full.txt. */
+            includeInLlms: z.ZodDefault<z.ZodBoolean>;
+            /** Include generated pages from this spec in site search. */
+            includeInSearch: z.ZodDefault<z.ZodBoolean>;
             /** Nav/section label for this source. */
             label: z.ZodOptional<z.ZodString>;
+            /** Emit noindex metadata and omit generated pages from the sitemap. */
+            noindex: z.ZodDefault<z.ZodBoolean>;
             /** Per-source route; defaults to the block's `route` (or a derived path). */
             route: z.ZodOptional<z.ZodString>;
             /** Local path or `http(s)` URL to the spec. */
             spec: z.ZodString;
         }, "strict", z.ZodTypeAny, {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -1333,6 +1396,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }>, "many">>;
         spec: z.ZodOptional<z.ZodString>;
         theme: z.ZodOptional<z.ZodString>;
@@ -1340,6 +1406,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
         enabled: boolean;
         route: string;
         sources: {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -1356,6 +1425,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }[] | undefined;
         theme?: string | undefined;
     }>>;
@@ -2322,13 +2394,22 @@ export declare const blumeConfigSchema: z.ZodObject<{
         scalar: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodUnknown>>;
         /** One or more specs; each renders on its own route by default. */
         sources: z.ZodDefault<z.ZodArray<z.ZodObject<{
+            /** Include generated pages from this spec in llms.txt/llms-full.txt. */
+            includeInLlms: z.ZodDefault<z.ZodBoolean>;
+            /** Include generated pages from this spec in site search. */
+            includeInSearch: z.ZodDefault<z.ZodBoolean>;
             /** Nav/section label for this source. */
             label: z.ZodOptional<z.ZodString>;
+            /** Emit noindex metadata and omit generated pages from the sitemap. */
+            noindex: z.ZodDefault<z.ZodBoolean>;
             /** Per-source route; defaults to the block's `route` (or a derived path). */
             route: z.ZodOptional<z.ZodString>;
             /** Local path or `http(s)` URL to the spec. */
             spec: z.ZodString;
         }, "strict", z.ZodTypeAny, {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -2336,6 +2417,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }>, "many">>;
         /** Shorthand for a single source: `sources: [{ spec }]`. */
         spec: z.ZodOptional<z.ZodString>;
@@ -2345,6 +2429,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
         enabled: boolean;
         route: string;
         sources: {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -2364,6 +2451,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }[] | undefined;
         theme?: string | undefined;
         codeSamples?: string[] | undefined;
@@ -2430,12 +2520,12 @@ export declare const blumeConfigSchema: z.ZodObject<{
             /** Index id used by the build-time sync (with `ORAMA_PRIVATE_API_KEY`). */
             indexId: z.ZodOptional<z.ZodString>;
         }, "strict", z.ZodTypeAny, {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         }, {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         }>>;
         /** Curated links for the Cmd+K empty state; defaults to the first sidebar pages. */
@@ -2491,8 +2581,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         typesense?: {
@@ -2516,8 +2606,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         popular?: {
@@ -2551,8 +2641,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         typesense?: {
@@ -2576,8 +2666,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         popular?: {
@@ -2958,6 +3048,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
         enabled: boolean;
         route: string;
         sources: {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -2973,6 +3066,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
         enabled: boolean;
         route: string;
         sources: {
+            includeInLlms: boolean;
+            includeInSearch: boolean;
+            noindex: boolean;
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
@@ -3003,6 +3099,7 @@ export declare const blumeConfigSchema: z.ZodObject<{
             }[];
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
+            endpoint?: string | undefined;
         } | undefined;
     };
     content: {
@@ -3205,8 +3302,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         typesense?: {
@@ -3319,6 +3416,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }[] | undefined;
         theme?: string | undefined;
         codeSamples?: string[] | undefined;
@@ -3334,6 +3434,9 @@ export declare const blumeConfigSchema: z.ZodObject<{
             spec: string;
             label?: string | undefined;
             route?: string | undefined;
+            includeInLlms?: boolean | undefined;
+            includeInSearch?: boolean | undefined;
+            noindex?: boolean | undefined;
         }[] | undefined;
         theme?: string | undefined;
     } | undefined;
@@ -3342,6 +3445,7 @@ export declare const blumeConfigSchema: z.ZodObject<{
             apiKeyEnv?: string | undefined;
             baseUrl?: string | undefined;
             enabled?: boolean | undefined;
+            endpoint?: string | undefined;
             model?: string | undefined;
             provider?: "gateway" | "openrouter" | "llmgateway" | "inkeep" | "openai-compatible" | undefined;
             suggestions?: {
@@ -3606,8 +3710,8 @@ export declare const blumeConfigSchema: z.ZodObject<{
             storeId: string;
         } | undefined;
         oramaCloud?: {
-            apiKey: string;
             endpoint: string;
+            apiKey: string;
             indexId?: string | undefined;
         } | undefined;
         popular?: {
diff --git a/dist/types/openapi/references.d.ts b/dist/types/openapi/references.d.ts
index dcdeae4da952484acf7dd91f442b57ad963e326b..7d9bc74bb06ac1f755b348d337cc9afd0ae8052f 100644
--- a/dist/types/openapi/references.d.ts
+++ b/dist/types/openapi/references.d.ts
@@ -31,6 +31,12 @@ export interface ReferenceSource {
      */
     basePath: string;
     label: string;
+    /** Whether generated pages are included in llms.txt/llms-full.txt. */
+    includeInLlms: boolean;
+    /** Whether generated pages are included in site search. */
+    includeInSearch: boolean;
+    /** Whether generated pages emit noindex metadata and stay out of the sitemap. */
+    noindex: boolean;
     /** Local path or `http(s)` URL, verbatim from config. */
     spec: string;
     /** Per-block Scalar theme name override, if any (Scalar renderer only). */
diff --git a/src/ai/agent-readability.ts b/src/ai/agent-readability.ts
index f2f6366a9e4e6cbcb727986d4def450afe906b67..cb9d574b546b31768b4c12d0216f8c207a0d44b8 100644
--- a/src/ai/agent-readability.ts
+++ b/src/ai/agent-readability.ts
@@ -69,7 +69,7 @@ export const buildAgentReadability = (
     };
   }
   if (config.ai.ask?.enabled) {
-    artifacts.askApi = abs("/api/ask");
+    artifacts.askApi = config.ai.ask.endpoint ?? abs("/api/ask");
   }
   if (site && config.seo.sitemap) {
     artifacts.sitemap = abs("/sitemap.xml");
diff --git a/src/ai/llms.ts b/src/ai/llms.ts
index e248ee4a49a8089f1515351b3c224f0cd1122cf9..6caf2df000eb046a6a9ae87ece0d060fb5be473e 100644
--- a/src/ai/llms.ts
+++ b/src/ai/llms.ts
@@ -19,13 +19,18 @@ const pageUrl = (route: string, site?: string, base = ""): string => {
   return encodeURI(site ? `${site.replace(/\/$/u, "")}${path}` : path);
 };
 
-// Drafts, hidden, and `noindex` pages are excluded, matching the sitemap.
-// Generated API reference pages are excluded when `ai.llmsTxt.openapi` is off
-// (they arrive through the internal staged "openapi" source).
+// Drafts, hidden, and ordinary `noindex` pages are excluded. Generated API
+// references keep crawler visibility (`noindex`) separate from LLM visibility
+// (`ai.exclude`), and are excluded wholesale when `ai.llmsTxt.openapi` is off.
 const eligiblePages = (project: BlumeProject): PageRecord[] =>
   project.graph.pages.filter(
     (page) =>
-      !(page.meta.draft || page.meta.sidebar.hidden || page.meta.seo.noindex) &&
+      !(
+        page.meta.ai.exclude ||
+        page.meta.draft ||
+        page.meta.sidebar.hidden ||
+        (page.meta.seo.noindex && page.source.name !== "openapi")
+      ) &&
       (project.config.ai.llmsTxt.openapi || page.source.name !== "openapi")
   );
 
diff --git a/src/astro/generate.ts b/src/astro/generate.ts
index 1d81be355693609ee0dd030c29c98d7de9a38443..c31741deb05177c131d03abd7ec485adfcfb00b2 100644
--- a/src/astro/generate.ts
+++ b/src/astro/generate.ts
@@ -1085,7 +1085,10 @@ export const buildRuntimeData = (project: BlumeProject): string => {
       analytics: config.analytics ?? null,
       appleIcon: resolveAppleIcon(project),
       ask: config.ai.ask?.enabled
-        ? { suggestions: config.ai.ask.suggestions }
+        ? {
+            endpoint: config.ai.ask.endpoint ?? null,
+            suggestions: config.ai.ask.suggestions,
+          }
         : null,
       banner: resolveBanner(config),
       basePath: config.basePath,
@@ -1279,7 +1282,7 @@ const writeAskFiles = async (
   write: (path: string, content: string) => Promise<boolean>
 ): Promise<void> => {
   const { ask } = project.config.ai;
-  if (!ask?.enabled) {
+  if (!(ask?.enabled && !ask.endpoint)) {
     return;
   }
   const grounded = ask.provider !== "inkeep";
diff --git a/src/astro/templates.ts b/src/astro/templates.ts
index 59ee041ed5a863a729f01a831a8c7c4836f4034a..101bedd757c28c484e9cab5fddfde90d6c5a7634 100644
--- a/src/astro/templates.ts
+++ b/src/astro/templates.ts
@@ -138,7 +138,7 @@ export const runtimeDependencies = (options: {
   // (and the user installs) exactly the backend it uses — nothing more.
   deps.push(...searchProviderMeta(config.search.provider).runtimeDeps);
   // Ask AI's provider SDK, when its backend needs one (gateway uses core `ai`).
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     const askDep = askBackendRuntimeDep(config.ai.ask);
     if (askDep) {
       deps.push(askDep);
@@ -850,7 +850,11 @@ import data from "blume:data";
 const { strings } = Astro.props;
 ---
 
-<AskAI strings={strings ?? data.ui.ask} suggestions={data.config.ask?.suggestions ?? []} />
+<AskAI
+  endpoint={data.config.ask?.endpoint ?? undefined}
+  strings={strings ?? data.ui.ask}
+  suggestions={data.config.ask?.suggestions ?? []}
+/>
 `
     : `---
 // Generated by Blume. Do not edit.
@@ -1195,6 +1199,7 @@ export async function GET({ props }: { props: { title: string } }) {
 export const scalarReferenceTemplate = (options: {
   configuration: Record<string, unknown>;
   dataImport: string;
+  noindex?: boolean;
   route: string;
   title: string;
 }): string =>
@@ -1229,6 +1234,7 @@ const htmlLang = i18n ? i18n.defaultLocale : "en";
   favicon={data.config.favicon}
   appleIcon={data.config.appleIcon}
   navigation={data.navigation}
+  noindex={${options.noindex === true}}
   pageTitle={${JSON.stringify(options.title)}}
   route={${JSON.stringify(options.route)}}
   searchEnabled={data.config.search.enabled}
diff --git a/src/cli/required-secrets.ts b/src/cli/required-secrets.ts
index 712804c2f35f1806ec23a15580a7a097e60a53a9..8834164ee15e4dadc1e030009338ead8affb0ed2 100644
--- a/src/cli/required-secrets.ts
+++ b/src/cli/required-secrets.ts
@@ -24,7 +24,7 @@ export const checkRequiredSecrets = (config: ResolvedConfig): Diagnostic[] => {
     });
   };
 
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     const backend = resolveAskBackend(config.ai.ask);
     if (backend.kind === "gateway") {
       requireSecret(
diff --git a/src/components/islands/AskAI.astro b/src/components/islands/AskAI.astro
index 3afd1a7db200fde7951730e69349b31211b9add1..5991f4ff4615ef421173707c4c5e9752f0359976 100644
--- a/src/components/islands/AskAI.astro
+++ b/src/components/islands/AskAI.astro
@@ -9,11 +9,12 @@ interface Suggestion {
 }
 
 interface Props {
+  endpoint?: string;
   strings?: UIStrings["ask"];
   suggestions?: Suggestion[];
 }
 
-const { strings, suggestions = [] } = Astro.props;
+const { endpoint, strings, suggestions = [] } = Astro.props;
 
 // Icons resolve to inline SVG here (server-only module), so the client island
 // gets ready-to-render markup rather than a name it can't resolve. Bare Lucide
@@ -43,7 +44,13 @@ const icons = {
 };
 ---
 
-<AskAI client:load icons={icons} strings={strings} suggestions={items} />
+<AskAI
+  client:load
+  endpoint={endpoint}
+  icons={icons}
+  strings={strings}
+  suggestions={items}
+/>
 
 <style is:global>
   :root {
diff --git a/src/components/islands/ask-ai.tsx b/src/components/islands/ask-ai.tsx
index 945e023fbade15ba19f38e47047d6f51a3365cb8..0f3b51683434a1bf5989acebda6d7b5b90b234b2 100644
--- a/src/components/islands/ask-ai.tsx
+++ b/src/components/islands/ask-ai.tsx
@@ -65,7 +65,7 @@ const nextId = (): number => {
 
 // The endpoint and page path both honor the deployment `base` so grounding works
 // under a non-root base path (the server matches base-less document routes).
-const ASK_ENDPOINT = joinBase(import.meta.env.BASE_URL, "api/ask");
+const DEFAULT_ASK_ENDPOINT = joinBase(import.meta.env.BASE_URL, "api/ask");
 
 /** The current route with the deployment base stripped, for page-context lookup. */
 const currentPath = (): string =>
@@ -133,10 +133,12 @@ const ANSWER_CLASS =
   "prose prose-sm max-w-none text-foreground [&_a]:inline-flex [&_a]:items-center [&_a]:gap-1 [&_a]:rounded-full [&_a]:bg-muted [&_a]:px-2 [&_a]:py-1 [&_a]:align-middle [&_a]:font-medium [&_a]:text-[0.7rem] [&_a]:leading-none [&_a]:text-muted-foreground! [&_a]:no-underline! [&_a:hover]:text-foreground!";
 
 const AskAI = ({
+  endpoint = DEFAULT_ASK_ENDPOINT,
   icons = EMPTY_ICONS,
   strings,
   suggestions = EMPTY_SUGGESTIONS,
 }: {
+  endpoint?: string;
   icons?: AskIcons;
   strings?: UIStrings["ask"];
   suggestions?: Suggestion[];
@@ -268,7 +270,7 @@ const AskAI = ({
     abortRef.current = controller;
 
     try {
-      const response = await fetch(ASK_ENDPOINT, {
+      const response = await fetch(endpoint, {
         body: JSON.stringify({
           messages: history.map((m) => ({ content: m.content, role: m.role })),
           page: { path: currentPath() },
diff --git a/src/components/islands/hooks.ts b/src/components/islands/hooks.ts
index 44d71c974cc8e1374d5c997bb1188f1424505a3e..4a9106dd73be119270b929d3818019a1922ce9d0 100644
--- a/src/components/islands/hooks.ts
+++ b/src/components/islands/hooks.ts
@@ -139,7 +139,12 @@ export interface UseAskAI {
   reset: () => void;
 }
 
-const ASK_ENDPOINT = joinBase(import.meta.env.BASE_URL, "api/ask");
+const DEFAULT_ASK_ENDPOINT = joinBase(import.meta.env.BASE_URL, "api/ask");
+
+export interface UseAskAIOptions {
+  /** Existing Ask AI endpoint; defaults to Blume's generated `/api/ask`. */
+  endpoint?: string;
+}
 
 /** Shown as the assistant's answer when the request fails or throws. */
 const ASK_ERROR = "Something went wrong answering that. Please try again.";
@@ -152,7 +157,8 @@ const currentPath = (): string =>
  * Stream answers from the Ask AI endpoint. Mirrors the built-in Ask AI island so
  * a custom chat UI shares the same grounded, page-aware backend.
  */
-export const useAskAI = (): UseAskAI => {
+export const useAskAI = (options: UseAskAIOptions = {}): UseAskAI => {
+  const endpoint = options.endpoint ?? DEFAULT_ASK_ENDPOINT;
   const [messages, setMessages] = useState<AskMessage[]>([]);
   const [loading, setLoading] = useState(false);
   // The stream writes into the conversation via state updates, so `reset()`
@@ -186,7 +192,7 @@ export const useAskAI = (): UseAskAI => {
       setMessages([...history, assistant]);
       setLoading(true);
       try {
-        const response = await fetch(ASK_ENDPOINT, {
+        const response = await fetch(endpoint, {
           body: JSON.stringify({
             messages: history,
             page: { path: currentPath() },
@@ -239,7 +245,7 @@ export const useAskAI = (): UseAskAI => {
         }
       }
     },
-    [loading, messages]
+    [endpoint, loading, messages]
   );
 
   // Retained for the compiler-off opt-out path (`react: { compiler: false }`):
diff --git a/src/components/layout/ReferenceLayout.astro b/src/components/layout/ReferenceLayout.astro
index 571c88d3a761328cfb532c8735af6a2bd9a351ff..f22dced2a90cf688791e7015194cdb7dc93d92a4 100644
--- a/src/components/layout/ReferenceLayout.astro
+++ b/src/components/layout/ReferenceLayout.astro
@@ -53,6 +53,8 @@ interface Props {
   fontCssVars?: string[];
   searchEnabled: boolean;
   pageTitle: string;
+  /** Keep the reference route out of crawler indexes. */
+  noindex?: boolean;
   /** Active locale code for `<html lang>` (defaults to `en`). */
   locale?: string;
   /** Text direction for `<html dir>` (defaults to `ltr`). */
@@ -74,6 +76,7 @@ const {
   fontCssVars,
   searchEnabled,
   pageTitle,
+  noindex = false,
   locale = "en",
   dir = "ltr",
   ui,
@@ -89,6 +92,7 @@ const bannerKey = banner?.dismissible ? banner.key : null;
   <head>
     <meta charset="utf-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1" />
+    {noindex && <meta name="robots" content="noindex" />}
     <title>{pageTitle}</title>
     <Favicon favicon={favicon} appleIcon={appleIcon} />
     <Fonts cssVars={fontCssVars ?? []} />
diff --git a/src/components/layout/RootLayout.astro b/src/components/layout/RootLayout.astro
index d6d86d7f6bc35996bc4e9f4708a7209fbf807924..9eb44829c5e992848a20e3403e2af10c941958b0 100644
--- a/src/components/layout/RootLayout.astro
+++ b/src/components/layout/RootLayout.astro
@@ -359,7 +359,7 @@ const bannerKey = banner?.dismissible ? banner.key : null;
 ---
 
 <!doctype html>
-<html dir={dir} lang={locale}>
+<html data-pagefind-ignore={indexable ? undefined : "all"} dir={dir} lang={locale}>
   <head>
     <meta charset="utf-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1" />
diff --git a/src/core/config-input.ts b/src/core/config-input.ts
index 0a3914a82a3153aa554d4cd4dd2a217e16d624e3..f79854fdaa8424ed9a65e6b004e530dba4e51b7a 100644
--- a/src/core/config-input.ts
+++ b/src/core/config-input.ts
@@ -522,6 +522,12 @@ export interface AskConfig {
   baseUrl?: string;
   /** Turn Ask AI on. Defaults to `false`. */
   enabled?: boolean;
+  /**
+   * Existing Ask AI endpoint to call instead of generating one. This keeps a
+   * Blume site static while an API backend owns retrieval, model access, rate
+   * limiting, and streaming. Accepts an absolute URL or root-relative path.
+   */
+  endpoint?: string;
   /** Model id to use. Defaults to `openai/gpt-5.5`. */
   model?: string;
   /** Which backend routes the request. Defaults to `gateway`. */
diff --git a/src/core/data.ts b/src/core/data.ts
index 30e0982e4b9948c04bfab858ac7a00b45f5c45ef..938a496753662f02c2f3e7e7e64c5890627cd0b1 100644
--- a/src/core/data.ts
+++ b/src/core/data.ts
@@ -95,6 +95,7 @@ export interface BlumeDataConfig {
   appleIcon: BlumeFavicon | null;
   /** Ask AI empty-state suggestions, or `null` when Ask AI is off. */
   ask: {
+    endpoint: string | null;
     suggestions: NonNullable<ResolvedConfig["ai"]["ask"]>["suggestions"];
   } | null;
   banner: BlumeBanner | null;
diff --git a/src/core/schema.ts b/src/core/schema.ts
index cb67a552d1b71a74a1506de6159982fc4cad6394..d5ef6af675757e0b4863682cde48e36450302e8e 100644
--- a/src/core/schema.ts
+++ b/src/core/schema.ts
@@ -78,6 +78,11 @@ const searchMetaSchema = z.strictObject({
   tags: z.array(z.string()).optional(),
 });
 
+const aiMetaSchema = z.strictObject({
+  /** Exclude this page from llms.txt and llms-full.txt. */
+  exclude: z.boolean().default(false),
+});
+
 const changelogMetaSchema = z.strictObject({
   category: z.string().optional(),
   date: dateSchema.optional(),
@@ -105,6 +110,7 @@ const authorSchema = z.union([
 
 /** Frontmatter accepted on any content page. */
 const pageMetaBaseSchema = z.strictObject({
+  ai: aiMetaSchema.default({}),
   /** Post author(s) for blog/changelog content; preserved, not yet rendered. */
   authors: z.union([authorSchema, z.array(authorSchema)]).optional(),
   changelog: changelogMetaSchema.optional(),
@@ -575,18 +581,44 @@ const mcpConfigSchema = z.strictObject({
   route: z.string().default("/mcp").transform(normalizeRoute),
 });
 
+const askEndpointSchema = z
+  .string()
+  .trim()
+  .min(1)
+  .refine(
+    (value) => {
+      if (value.startsWith("/") && !value.startsWith("//")) {
+        return true;
+      }
+      try {
+        const url = new URL(value);
+        return url.protocol === "http:" || url.protocol === "https:";
+      } catch {
+        return false;
+      }
+    },
+    {
+      message:
+        "ai.ask.endpoint must be an HTTP(S) URL or a root-relative path.",
+    }
+  );
+
 const aiConfigSchema = z.strictObject({
   ask: z
     .strictObject({
       // Name of the env var holding the provider's API key; each provider has
       // a sensible default, so this only needs setting to override it.
       apiKeyEnv: z.string().optional(),
-      // Base URL of the backend. Required for `openai-compatible`; for the
-      // named providers it overrides the built-in preset.
+      // Base URL of the backend. Required for `openai-compatible` only when no
+      // external endpoint is supplied; for named providers it overrides the preset.
       // blume bundles Zod 3; top-level `z.url()` is undefined at runtime.
       // oxlint-disable-next-line react-doctor/zod-v4-prefer-top-level-string-formats
       baseUrl: z.string().url().optional(),
       enabled: z.boolean().default(false),
+      // Optional external endpoint for projects that keep their docs static
+      // and host Ask AI in an existing backend. Absolute URLs and root-relative
+      // paths are both valid; the built-in request/stream contract is unchanged.
+      endpoint: askEndpointSchema.optional(),
       model: z.string().default("openai/gpt-5.5"),
       provider: z.enum(askAiProviders).default("gateway"),
       // Empty-state prompts shown before the first question. Each renders as a
@@ -603,7 +635,10 @@ const aiConfigSchema = z.strictObject({
     .superRefine((value, ctx) => {
       // A generic OpenAI-compatible backend has no preset URL, so the user
       // must supply one; the named providers fall back to their preset.
-      if (value.provider === "openai-compatible" && !value.baseUrl) {
+      if (
+        value.provider === "openai-compatible" &&
+        !(value.baseUrl || value.endpoint)
+      ) {
         ctx.addIssue({
           code: z.ZodIssueCode.custom,
           message:
@@ -1118,15 +1153,21 @@ const reactConfigSchema = z.strictObject({
  * `http(s)` URL (OpenAPI for the Blume renderer; OpenAPI or AsyncAPI for Scalar).
  */
 const openapiSourceSchema = z.strictObject({
+  /** Include generated pages from this spec in llms.txt/llms-full.txt. */
+  includeInLlms: z.boolean().default(true),
+  /** Include generated pages from this spec in site search. */
+  includeInSearch: z.boolean().default(true),
   /** Nav/section label for this source. */
   label: z.string().optional(),
+  /** Emit noindex metadata and omit generated pages from the sitemap. */
+  noindex: z.boolean().default(false),
   /** Per-source route; defaults to the block's `route` (or a derived path). */
   route: z.string().optional(),
   /** Local path or `http(s)` URL to the spec. */
   spec: z.string(),
 });
 
-export type OpenApiSource = z.infer<typeof openapiSourceSchema>;
+export type OpenApiSource = z.input<typeof openapiSourceSchema>;
 
 /**
  * Arbitrary Scalar API-reference options forwarded verbatim to the generated
diff --git a/src/core/server-features.ts b/src/core/server-features.ts
index f8fd7148a0b4170041aaf995c9b8018ed3e9ba4a..ee8ac7ac9c84d9446e2e6552a69afc8db45fd1c0 100644
--- a/src/core/server-features.ts
+++ b/src/core/server-features.ts
@@ -7,7 +7,7 @@ import type { ResolvedConfig } from "./schema.ts";
  */
 export const serverFeatures = (config: ResolvedConfig): string[] => {
   const features: string[] = [];
-  if (config.ai.ask?.enabled) {
+  if (config.ai.ask?.enabled && !config.ai.ask.endpoint) {
     features.push("Ask AI");
   }
   // The hosted MCP server is a live JSON-RPC endpoint, so it needs a runtime.
diff --git a/src/openapi/references.ts b/src/openapi/references.ts
index a5be50d2808c14f6e433a38775c8741dbd558f6f..8c2a93caa0ce0bd823a868f4f8ee3f06f9f4fd70 100644
--- a/src/openapi/references.ts
+++ b/src/openapi/references.ts
@@ -38,6 +38,12 @@ export interface ReferenceSource {
    */
   basePath: string;
   label: string;
+  /** Whether generated pages are included in llms.txt/llms-full.txt. */
+  includeInLlms: boolean;
+  /** Whether generated pages are included in site search. */
+  includeInSearch: boolean;
+  /** Whether generated pages emit noindex metadata and stay out of the sitemap. */
+  noindex: boolean;
   /** Local path or `http(s)` URL, verbatim from config. */
   spec: string;
   /** Per-block Scalar theme name override, if any (Scalar renderer only). */
@@ -78,10 +84,22 @@ type Block = ResolvedConfig["openapi"] | ResolvedConfig["asyncapi"];
 /** A spec is a single source (`spec` shorthand prepended to any `sources`). */
 const sourcesOf = (
   block: Block
-): { label?: string; route?: string; spec: string }[] => {
+): {
+  includeInLlms: boolean;
+  includeInSearch: boolean;
+  label?: string;
+  noindex: boolean;
+  route?: string;
+  spec: string;
+}[] => {
   const sources = [...block.sources];
   if (block.spec) {
-    sources.unshift({ spec: block.spec });
+    sources.unshift({
+      includeInLlms: true,
+      includeInSearch: true,
+      noindex: false,
+      spec: block.spec,
+    });
   }
   return sources;
 };
@@ -118,8 +136,11 @@ const referencesFor = (
     return {
       basePath,
       display,
+      includeInLlms: source.includeInLlms,
+      includeInSearch: source.includeInSearch,
       kind,
       label,
+      noindex: source.noindex,
       renderer,
       route,
       scalar: block.scalar,
diff --git a/src/openapi/render-mdx.ts b/src/openapi/render-mdx.ts
index 087e52434f175d001fd0108910054a6788f218c2..b453e05371a398075a85a7af0044e54da12f3a3a 100644
--- a/src/openapi/render-mdx.ts
+++ b/src/openapi/render-mdx.ts
@@ -1,4 +1,5 @@
 import type { ApiOperationRef, ApiSpecData } from "./model.ts";
+import type { ReferenceSource } from "./references.ts";
 
 /**
  * Lower a parsed spec into MDX for the staged content source. Each operation and
@@ -126,7 +127,11 @@ const withDescription = (description: string, component: string): string =>
 
 export const operationMdx = (
   spec: ApiSpecData,
-  operation: ApiOperationRef
+  operation: ApiOperationRef,
+  reference?: Pick<
+    ReferenceSource,
+    "includeInLlms" | "includeInSearch" | "noindex"
+  >
 ): RenderedPage => {
   const method = operation.method.toUpperCase();
   const title = operation.summary || `${method} ${operation.path}`;
@@ -142,9 +147,16 @@ export const operationMdx = (
       `<Operation source="${spec.slug}" id="${operation.key}" />`
     ),
     data: {
+      ...(reference?.includeInLlms === false ? { ai: { exclude: true } } : {}),
       ...(operation.deprecated ? { deprecated: true } : {}),
-      search: { tags: [operation.tag, method] },
-      seo: { description: operationDescription(spec, operation) },
+      search: {
+        ...(reference?.includeInSearch === false ? { exclude: true } : {}),
+        tags: [operation.tag, method],
+      },
+      seo: {
+        description: operationDescription(spec, operation),
+        ...(reference?.noindex ? { noindex: true } : {}),
+      },
       sidebar: { badge: method, label: operation.summary || operation.path },
       title,
       // Signals the two-column API layout (request panel instead of the TOC).
@@ -153,7 +165,13 @@ export const operationMdx = (
   };
 };
 
-export const overviewMdx = (spec: ApiSpecData): RenderedPage => {
+export const overviewMdx = (
+  spec: ApiSpecData,
+  reference?: Pick<
+    ReferenceSource,
+    "includeInLlms" | "includeInSearch" | "noindex"
+  >
+): RenderedPage => {
   // Tag sections: declared tags in spec order, then any tag an operation
   // references that isn't declared under `tags`. The section headings are
   // emitted as real markdown `##` (not markup inside a component) so the
@@ -205,10 +223,15 @@ export const overviewMdx = (spec: ApiSpecData): RenderedPage => {
       ...tagSections,
     ].join("\n\n"),
     data: {
+      ...(reference?.includeInLlms === false ? { ai: { exclude: true } } : {}),
+      ...(reference?.includeInSearch === false
+        ? { search: { exclude: true } }
+        : {}),
       seo: {
         description:
           clip(plainProse(spec.description), META_DESCRIPTION_MAX) ||
           `${apiName(spec)} API reference.`,
+        ...(reference?.noindex ? { noindex: true } : {}),
       },
       sidebar: { label: "Overview" },
       title: apiName(spec),
diff --git a/src/openapi/scalar.ts b/src/openapi/scalar.ts
index b242e3d22aba83e347941db8ee1e183ded25cc10..34f30292f7bd65b7f6e0503a700239bab664fc5b 100644
--- a/src/openapi/scalar.ts
+++ b/src/openapi/scalar.ts
@@ -167,6 +167,7 @@ export const buildReferenceFiles = async (options: {
           ...ref.scalar,
         },
         dataImport: `${"../".repeat(depth + 1)}generated/data.json`,
+        noindex: ref.noindex,
         route: ref.route,
         title: ref.label,
       }),
diff --git a/src/openapi/source.ts b/src/openapi/source.ts
index 3ab4db90779c57d81eac14233509796f59d28ecf..b8a31c38e5538ba1ba05b7519a827b311d618e26 100644
--- a/src/openapi/source.ts
+++ b/src/openapi/source.ts
@@ -53,17 +53,24 @@ const toEntry = (rendered: RenderedPage, ref: string): SourceEntry => {
 /** All staged entries for one spec: operations first, overview last. */
 const specEntries = (
   spec: ApiSpecData,
-  operations: ApiOperationRef[]
+  operations: ApiOperationRef[],
+  reference: ReferenceSource
 ): SourceEntry[] => {
   const entries = operations.map((operation) =>
-    toEntry(operationMdx(spec, operation), `${routeToRef(operation.route)}.mdx`)
+    toEntry(
+      operationMdx(spec, operation, reference),
+      `${routeToRef(operation.route)}.mdx`
+    )
   );
   // Overview last so an operation sets the section's routePath before the index
   // page is inserted (the group's routePath is derived from its first child).
   // A root-mounted reference refs `index.mdx`, not `/index.mdx`.
   const base = routeToRef(spec.route);
   entries.push(
-    toEntry(overviewMdx(spec), base ? `${base}/index.mdx` : "index.mdx")
+    toEntry(
+      overviewMdx(spec, reference),
+      base ? `${base}/index.mdx` : "index.mdx"
+    )
   );
   return entries;
 };
@@ -149,7 +156,7 @@ export const openApiSource = (
               ]
             : []),
         ],
-        entries: specEntries(spec, operations),
+        entries: specEntries(spec, operations, reference),
         slug: reference.slug,
         spec,
       };
diff --git a/src/registry/eject.ts b/src/registry/eject.ts
index f86bc0bf23b35e0973f11cc554cd814819bcabf3..8b670822bb8f0c45ef6330093cdb7526020d6955 100644
--- a/src/registry/eject.ts
+++ b/src/registry/eject.ts
@@ -112,7 +112,15 @@ const askFiles = async (
   genDir: string
 ): Promise<{ content: string; path: string }[]> => {
   const { ask } = project.config.ai;
-  if (!ask?.enabled) {
+  if (!(ask?.enabled && !ask.endpoint)) {
+    const endpointPath = join(srcDir, "pages", "api", "ask.ts");
+    if (existsSync(endpointPath)) {
+      const content = await readFile(endpointPath, "utf-8");
+      if (content.startsWith("// Generated by Blume. Do not edit.")) {
+        await rm(endpointPath, { force: true });
+      }
+    }
+    await rm(join(genDir, "ask-data.json"), { force: true });
     return [];
   }
   const grounded = ask.provider !== "inkeep";
