---
title: Reference
sidebar:
  label: Overview
description: Exact commands, endpoints, errors, SDKs, and release history for Spacefast interfaces.
---

Use the guides when you learn a workflow. Use this section when you need the
exact contract.

<CardGroup cols={2}>
  <Card title="REST API" href="/api/reference" icon="braces">
    Generated endpoint schemas and request fields.
  </Card>
  <Card title="CLI" href="/cli" icon="terminal">
    Generated commands, flags, arguments, and examples.
  </Card>
  <Card title="Errors" href="/errors" icon="triangle-alert">
    Stable error codes, meanings, and recovery guidance.
  </Card>
  <Card title="Changelog" href="/changelog" icon="history">
    Product and package changes by release.
  </Card>
  <Card title="Platform API" href="/platforms/api/reference" icon="building-2">
    The tenant surface for hosting customer sites.
  </Card>
  <Card title="SDK" href="/reference/sdk" icon="package">
    Typed clients for application code.
  </Card>
</CardGroup>

For direct HTTP integration guidance, start with the [REST API guide](/api).
When something misbehaves, start with [Troubleshooting](#troubleshooting)
below. For agent tooling (MCP, skills, machine-readable docs), see
[Agents](/agents).

## Troubleshooting

Find the symptom below and run the check next to it.

### DNS diagnostics

Your domain does not resolve or does not serve the space. Check it, then pull
the detailed report:

```bash
sf domains check www.example.com
sf domains diagnostics www.example.com
```

If DNS fails, check that the hostname, record type, and target value exactly
match the space instructions.

### Missing index

If the homepage is missing, check that your folder or zip has `index.html` at
the root. SPA mode also requires root `index.html`.

### Upload or finalize failures

Retry the publish. The CLI matches files by `sha256` and size, so it never
re-uploads an unchanged file. When Spacefast rejects a publish because it
exceeds the storage quota, the live site is not affected. Delete old versions
or add storage. Then publish again.

### Rollback recovery

A bad publish never takes the live site down. Promote a known-good version to
recover.

```bash
sf versions ls
sf rollback v12
```

### Version URL asks for sign-in

The live URL is not affected. The team's current entitlements decide whether
older immutable version URLs remain public. See
[Roll back](/publishing/channels#roll-back).

### Password prompts

A password wall on every page means space password protection is on. Spacefast
does not support `_headers` Basic Auth; path-level protection comes from
sharing rules. See [Passwords & access](/spaces/access).

### Local CLI setup

`sf doctor` checks local state, auth, API reachability, and the current space
link.
